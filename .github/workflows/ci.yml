name: CI
on:
  pull_request:
  push:
jobs:
  test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
        - java: 8
          os: ubuntu-latest
        - java: 21
          os: ubuntu-latest
        - java: 24-ea
          os: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      with:
        java-version: ${{ matrix.java }}
        distribution: temurin
    - uses: coursier/cache-action@v6
    - run: |
        case "${{ matrix.java }}" in
          "8")
            sbt "+ Test/compile" test
            ;;
          *)
            sbt "+ Test/compile" test "+ scripted"
        esac
    - if: matrix.os != 'windows-latest'
      run: sbt scalafmtSbtCheck '+ scalafmtCheckAll'
    - run: rm -rf "${HOME}/.ivy2/local"
